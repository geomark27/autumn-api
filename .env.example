# ========================================
# AUTUMN BANKING SYSTEM - EXAMPLE ENVIRONMENT
# ========================================
# INSTRUCCIONES:
# 1. Copia este archivo como .env.local
# 2. Reemplaza los valores de ejemplo con tus credenciales reales
# 3. NUNCA subas .env.local a Git

# PostgreSQL Database
POSTGRES_DB=your_database_name
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_PORT=5432

# Redis Cache
REDIS_PORT=6379

# Spring Boot Application
SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8080

# JWT Security (genera una clave segura de al menos 256 bits)
JWT_SECRET=change-this-to-a-secure-random-string-minimum-256-bits
JWT_EXPIRATION=86400000

# Logging Level (DEBUG, INFO, WARN, ERROR)
LOGGING_LEVEL=INFO
